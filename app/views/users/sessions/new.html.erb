<h2>Log in</h2>
<div class="row">
  <div class="col-10">
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email" } %>
        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password" } %>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Log in" %>
      </div>
    <% end %>

    <%= render "users/shared/links" %>
  </div>
  <div class="col-2">
    <div class="card">
      <div class="card-header">
        Demo User (copy paste)
      </div>
      <div class="card-body">
        <p class="card-text"><strong>Email: </strong>contact.luqman.khan@gmail.com</p>
        <p class="card-text"><strong>Password: </strong>domazait</p>
      </div>
    </div>
  </div>
</div>
